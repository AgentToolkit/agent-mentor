import os
from dotenv import load_dotenv
load_dotenv()

root_dir = os.getenv('PROJECT_ROOT')
#task_flow_dir = os.path.join(root_dir, 'data','flows','task')
#prompt_dir = os.path.join(root_dir, 'src','prompts')
