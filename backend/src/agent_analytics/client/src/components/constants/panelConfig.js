// Panel configuration constants
export const DEFAULT_PANEL_CONFIG = {
  sidePanel: {
    minWidth: 400,
    maxWidth: 800,
    defaultWidth: 420,
    persistState: true, // Set to true to save panel state to localStorage
  },
  topPanel: {
    collapsible: true,
    showMinimalInfo: true,
  },
};
